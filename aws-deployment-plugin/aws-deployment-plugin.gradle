plugins {
    id "groovy"
    id "java-gradle-plugin"
    id "maven-publish"
}

sourceCompatibility = '1.8'
targetCompatibility = '1.8'
version = '1.0'

dependencies{
    compile project(":deployment-plugin")
}
